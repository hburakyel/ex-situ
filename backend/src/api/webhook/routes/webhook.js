// Webhook endpoint REMOVED — it exposed the entire database without authentication.
// If a webhook is needed in the future, add proper authentication (API key or JWT).
module.exports = {
    routes: [],
  };

  